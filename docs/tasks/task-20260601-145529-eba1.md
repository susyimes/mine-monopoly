# task-20260601-145529-eba1

## 概览

- 标题：原型:去品牌化「大富翁」AI 直播(阶段0闭环)
- 状态：active
- 事实来源：`github-task-list`
- 任务源文件：`tasks/2026/2026-06-01/task-20260601-145529-eba1.md`
- 任务远端索引：https://github.com/susyimes/github-task-list/blob/main/tasks/2026/2026-06-01/task-20260601-145529-eba1.md
- 项目仓库：https://github.com/susyimes/mine-monopoly-ai-live

本文件是项目内任务镜像，用于把工程文档、代码位置和 `github-task-list` 任务记录互相连起来。任务正文、状态流转和后续 `Next` 仍以 `github-task-list` 为准。

## 项目文档

- [阶段 1 文档](../phase1-ai-live-monopoly.md)
- [AI 地产棋局改造方案](../ai-live-remodel-plan.md)
- [Agent 群落运行时](../agent-colony-runtime.md)

## 当前 Next

阶段1-M2/M2.5 已完成动作落地和 agent 群落抽象验收；混合真人局验收当前取消，暂不作为后续计划。下一步转入内容/直播前的独立评估项：是否要把 memsuOS 候选记忆写入打开为默认流程，以及是否需要专门做机会卡策略。

- 保留当前 `tools/ai-live` 的 Playwright browser-context 路线。
- 继续暂缓内容、直播、TTS、OBS 和弹幕互动。
- Mimo 配置使用本机 `D:\memsuOS\.memsuos\model-providers.local.json`，密钥只运行时读取，不进入仓库。

## 已完成节点

- 在 `D:\mine-monopoly` 完成阶段 0 本地可玩闭环：MySQL、用户服务、游戏服务、PeerJS 信令和三个前端均可运行。
- 验证架构边界：RoomRouter 只登记房间与房主 Peer，PeerServer 只做 PeerJS 信令，房主浏览器持有 `GameProcessWorker` 和主要规则循环。
- 创建阶段 1 文档，明确自动房间、bot-runner、规则策略、LLM 策略适配和旁白后续边界。
- 完成第一轮用户可见去品牌化：登录页、客户端、管理台标题与旧外链文案已清理。
- 组织 4 个子 agent 做技术架构、bot 协议、产品玩法、工程落地评估，并沉淀改造方案。
- 导出扁平化新仓库 `D:\mine-monopoly-ai-live`，创建私有远端并推送初始提交。
- 在 `tools/ai-live` 落地阶段 1-M1：`stack:check`、Playwright 浏览器池、房主建房/选图/设置回合、4 bot 加入 ready、混合局等待配置、规则策略接口、结构化日志、运行报告和 smoke 测试。
- 本地验证：`npm run stack:check` 通过；`npm run test:smoke` 通过；`npm run run:rules -- --room aim1ok --bots 4 --humans 0 --headful=false --round-time 5 --max-run-ms 30000` 通过，生成 22 条事件、6 条决策、0 错误和 2 张截图。
- 接入 `--policy mimo`，读取 memsuOS 的本机 Mimo provider 配置并调用真实 Mimo chat/completions；本地验证 `npm run run:rules -- --room mimo4t4 --bots 4 --humans 0 --policy mimo --headful=false --round-time 5 --max-run-ms 90000 --client-url http://127.0.0.1 --mimo-config D:\memsuOS\.memsuos\model-providers.local.json --mimo-timeout-ms 20000` 通过，生成 31 条事件、13 条决策、2 张截图；多数决策来自 Mimo 中文理由，1 次请求失败按设计回落规则。
- 给客户端登录页补 `?automation=1` 快速入口，并加固 `tools/ai-live` 的 `/room-router` hash 导航，避免登录动画或 Vite 首次加载导致自动化错过入口。
- 完成阶段1-M2 动作落地桥接：客户端 `?automation=1` 暴露 `gameInitFinished`、`rollDice`、`buyProperty`、`buildHouse`、`animationComplete` 协议桥，bot 优先通过 bridge 驱动真实动作并记录消息；本轮实测观测到投骰、买地和动画完成，`buildHouse` 等待长局或定向场景触发复核。
- 规则策略验收：`npm run run:rules -- --room m2rules5 --bots 4 --humans 0 --policy rules --headful=false --round-time 8 --max-run-ms 120000 --client-url http://localhost` 通过，生成 124 条事件、12 条决策、2 张截图；决策均来自 `automation-bridge` 且 `dispatched: true`，记录 36 次玩家移动和 32 次动画完成。
- Mimo 策略验收：`npm run run:rules -- --room m2mimo1 --bots 4 --humans 0 --policy mimo --headful=false --round-time 12 --max-run-ms 120000 --client-url http://localhost --mimo-config D:\memsuOS\.memsuos\model-providers.local.json --mimo-timeout-ms 12000` 通过，生成 91 条事件、7 条 Mimo 决策、2 张截图；决策均真实下发，记录 24 次玩家移动和 20 次动画完成。
- 完成 M2.5 初版 agent 群落抽象：新增 `tools/ai-live/src/colony` 通用运行时接口和 `tools/ai-live/src/adapters/monopoly/automation-bridge-adapter.ts`，`BotPlayer` 已通过 `AgentColonyRuntime` 接入 monopoly bridge；memsuOS 定位为人格、记忆、模型 provider 和长期状态底座，不承载具体游戏协议。
- M2.5 回归验收：`npm run run:rules -- --room colony1 --bots 4 --humans 0 --policy rules --headful=false --round-time 8 --max-run-ms 90000 --client-url http://localhost` 通过，生成 94 条事件、7 条决策、2 张截图、0 错误；决策均来自 `automation-bridge` 且 `dispatched: true`，记录 24 次玩家移动和 24 次动画完成。
- 按用户要求取消混合局当前计划：`--humans 1 --bots 3` 保留为工具兼容能力，但不再作为阶段 1 当前验收项。
- 完成远程资源降噪：`tools/ai-live` 将 COS/字体/静态路由 fallback 资源问题分类为 `asset.issue` 事件，不再污染 pageerror；同时房间路由改为 hash-first，避免静态服务器 `/room-router?automation=1` 404 噪声。
- 完成 `buildHouse` 定向复核：新增 `npm run test:build-house`，通过伪造 `BuildHouse` bridge 事件验证 `AgentColonyRuntime -> monopoly adapter -> buildHouse(accept)` 链路。
- 完成 memsuOS 最小端口：新增 `tools/ai-live/src/memsu/memory-port.ts`，默认关闭；设置 `AI_LIVE_MEMSU_MEMORY=1` 时可把对局复盘作为 `proposed_memory` 追加到 `D:\memsuOS\.memsuos\game-agent-memory.jsonl`。
- 收束验收：`npm run typecheck`、`npm run test:asset-issues`、`npm run test:build-house`、`corepack yarn build`、`npm run stack:check` 均通过；规则回归局 `finish2` 通过，生成 110 条事件、7 条决策、2 张截图、0 browser console、0 errors，记录 32 次玩家移动和 32 次动画完成。

## 下一步工程入口

下一步继续从以下目录推进：

```text
tools/ai-live/
  package.json
  src/
    stack-check.ts
    browser-room.ts
    bot-runner.ts
    personas.ts
```

阶段 1-M2/M2.5 暂不处理内容、直播、TTS、OBS 和弹幕互动；这些项留到规则/Mimo bot 与 agent 群落运行时稳定后再进入后续任务。
